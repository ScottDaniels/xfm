.** if there is room, at least $1 lines in the current column, pull in the file $2 now, else pull in at next eject
.** _lines is tripple escaped so that it evaluates as late as possible -- after the gv command is executed and not
.** when the macro is loaded for use. 
.dv ifroom .if pfm ^:  .gv lines .if  ^^^&_lines $1 >   ^:  .im $2  .ei  .oe col .im $2  .fi  .ei  .im $2  .fi
